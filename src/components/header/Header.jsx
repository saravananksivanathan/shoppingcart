import React from "react";

const Header = () => {
  const component = (
    <nav className="navbar navbar-dark bg-dark">
      <span className="navbar-brand">Masalas & Spices</span>
    </nav>
  );

  return component;
};

export default Header;
